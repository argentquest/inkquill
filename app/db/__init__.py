# /ai_rag_story_app/app/db/__init__.py

# This file marks the 'db' directory as a Python package.
# It allows you to import modules from this directory using dot notation
# (e.g., from app.db.database import Base, engine, async_session_local).

# This file can be kept empty.
# Optionally, you could import key components from modules within this package
# to make them available at a higher level, for example:
#
# from .database import Base, engine, async_session_local, init_db
#
# This would allow imports like:
# from app.db import Base
# instead of:
# from app.db.database import Base
#
# For now, keeping it empty is standard and clear.
