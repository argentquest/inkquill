# /ai_rag_story_app/app/routers/__init__.py

# This file marks the 'routers' directory as a Python package.
# It allows you to import modules from this directory using dot notation
# (e.g., from app.routers.auth import router as auth_router).

# This file can be kept empty.
# Optionally, you could import your APIRouter instances here if you
# wanted to aggregate them or make them available from app.routers directly,
# though this is less common as they are usually included in main.py.
#
# Example (not typically done this way for routers):
# from .auth import router as auth_router
# from .users import router as users_router
# ... etc.
#
# For now, keeping it empty is standard.

